# Docstrings

Google style, checked by pydoclint and ruff `D`.

- One line for one-liners; full `Args:` / `Returns:` / `Raises:` sections otherwise.
- Types live in the signature only — never repeated in the docstring.
- A module docstring states *why the file exists*, not its mechanics.
- Private helpers may keep a single summary line.
