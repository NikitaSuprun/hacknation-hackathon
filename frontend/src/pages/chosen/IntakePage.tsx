/** TODO(Track H): inbound founder intake — "Get on the radar" (GitHub/LinkedIn/project/team). */
export default function IntakePage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-paper px-gutter">
      <p className="mono-label mb-2">Chosen</p>
      <h1 className="font-display text-h1">Put yourself on the radar.</h1>
    </div>
  );
}
