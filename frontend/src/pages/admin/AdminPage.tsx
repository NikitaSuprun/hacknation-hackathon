/** TODO(Track G): database overview — table stats, people graph, detail panel, table browser. */
export default function AdminPage() {
  return (
    <div className="py-gutter-lg">
      <p className="mono-label mb-2">Admin</p>
      <h1 className="font-display text-h1">Database</h1>
    </div>
  );
}
